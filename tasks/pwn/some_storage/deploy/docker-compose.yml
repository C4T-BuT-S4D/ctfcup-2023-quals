version: "2.4"

services:
  pwn:
    build: .
    ports:
      - "13002:13002"
    restart: unless-stopped
    cpus: 1
    pids_limit: 64
    mem_limit: 512m
    environment:
      - FLAG=ctfcup{flag}